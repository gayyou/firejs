{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna clean --yes && rimraf packages/*/lib/*",
    "build": "ts-node scripts/build/build.ts"
  },
  "devDependencies": {
    "typescript": "^3.7.4",
    "@babel/parser": "^7.12.3",
    "@babel/types": "^7.12.1",
    "@babel/core": "^7.5.5",
    "jest": "^26.6.0",
    "lerna": "^3.22.1",
    "rimraf": "^3.0.0"
  },
  "dependencies": {
    "execa": "^4.0.3"
  }
}
